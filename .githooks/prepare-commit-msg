#!/bin/sh
# Strip Cursor co-author trailer injected by the IDE agent.
# Enable once: git config core.hooksPath .githooks
if [ -f "$1" ]; then
  grep -v 'Co-authored-by: Cursor <cursoragent@cursor.com>' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
